# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
python-multipart==0.0.20

# Database
motor==3.7.0
pymongo==4.11.3
beanie==1.27.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# ML - Core
scikit-learn==1.5.2
xgboost==2.1.3
joblib==1.4.2
numpy==2.2.1
pandas==2.2.3

# ML - NLP
transformers==4.47.1
torch>=2.0.0
torchvision>=0.15.0

# ML - Graph
networkx==3.4.2

# ML - Vision
opencv-python-headless==4.10.0.84
Pillow==11.1.0

# Utilities
httpx==0.28.1
loguru==0.7.3
email-validator>=2.1.0

SpeechRecognition==3.17.0

# ── CC Counterfeit Currency Detection Pipeline (added) ─────────────────────
# YOLOv11 security feature detection (falls back to CV engine if model not present)
ultralytics>=8.1.24
# EasyOCR serial number + text extraction (falls back to simulation if unavailable)
easyocr>=1.7.1
# Grad-CAM heatmap rendering
matplotlib>=3.8.3
# Image augmentation (used by ultralytics internally)
albumentations>=1.4.0
# ONNX runtime (used by ultralytics/easyocr for optimized inference)
onnxruntime>=1.17.1
# PostgreSQL support (optional — CC pipeline falls back gracefully if DB offline)
sqlalchemy>=2.0.28
psycopg2-binary>=2.9.9
